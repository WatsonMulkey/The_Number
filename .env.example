# ============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in all required values
# DO NOT commit .env to version control!

# CRITICAL: Database Encryption Key
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: Without this key, ALL USER DATA IS PERMANENTLY LOST
# BACKUP THIS KEY SECURELY (password manager, offline backup)
DB_ENCRYPTION_KEY=REQUIRED_CHANGE_THIS

# CRITICAL: JWT Secret for Authentication
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# WARNING: Changing this invalidates all existing user sessions
# BACKUP THIS KEY SECURELY
JWT_SECRET_KEY=REQUIRED_CHANGE_THIS

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# CORS Origins (comma-separated list of allowed frontend URLs)
# Development: http://localhost:5173,http://localhost:5174
# Production: https://foil.engineering
# IMPORTANT: Set this to your actual frontend URL(s) in production!
# Note: Only include the origin (protocol + domain), NOT the path
# Example: https://foil.engineering (correct) NOT https://foil.engineering/TheNumber (incorrect)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176

# Log Level (DEBUG, INFO, WARNING, ERROR)
# Use INFO or WARNING in production to avoid exposing sensitive data
LOG_LEVEL=INFO

# Database Path (optional, defaults to api/budget.db)
# For Railway deployment with volumes: /app/data/budget.db
# DB_PATH=api/budget.db

# Server Port (optional, defaults to 8000)
# PORT=8000

# ============================================================================
# BACKUP REMINDERS
# ============================================================================
# After generating keys:
# 1. Save .env file to password manager
# 2. Create offline backup (USB drive, printed copy in safe)
# 3. NEVER commit .env to git (already in .gitignore)
# 4. Test restore process before deploying to production
